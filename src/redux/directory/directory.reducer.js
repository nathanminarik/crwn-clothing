import { SECTIONS } from "../../mock-data";

const INITIAL_STATE = {
    sections: SECTIONS
}

export const directoryReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default: 
        return state;
    }
}
